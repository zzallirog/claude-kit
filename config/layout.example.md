# 🗺️ Laptop map (where) — what lives where

> Claude will fill this in / adjust it for you. Edit freely. Command: `where`.

## Where things are
- `~/projects/`   — side projects, each in its own folder (not piled together, not deduplicated)
- `~/kb/`         — knowledge base: notes, cheatsheets, "what broke and how I fixed it"
- `~/.config/`    — program configs (touch carefully)
- `~/.local/bin/` — kit commands (update, fix, where, recall, kit-help)
- `~/.local/state/kit-claude/` — kit logs + `unfixed.jsonl` (what couldn't be fixed)

## Commands (what to use for what)
- `where`     — this map
- `recall X`  — search notes and logs
- `update`    — update the system + security check
- `fix`       — repair: disk · DNS · Wi-Fi · packages · services
- `kit-help`  — cheatsheet
- `claude`    — ask the AI

## Who does what (briefly; in full — THE-PROTOCOL.md)
- **You** — owner, password, and the last word.
- **Opus (Architect)** — thinks, plans, keeps the map.
- **Sonnet (Operative)** — does the routine work; reaches root by grant.
- Userspace (`~/`) — free. The system (root) — only with your grant.

## My notes about this machine
<!-- here: where backups are, password manager, important quirks of your specific laptop -->
